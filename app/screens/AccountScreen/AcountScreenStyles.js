import { StyleSheet } from 'react-native';
import colors from '../../config/colors';

const AcountScreenStyles = StyleSheet.create({
  screen: {
    backgroundColor: colors.light,
  },
  container: {
    marginVertical: 20,
  },
});

export default AcountScreenStyles;
